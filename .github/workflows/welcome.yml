name: Welcome contributors

on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  issue:
    name: Welcome first-time issue authors
    if: >-
      github.event_name == 'issues' &&
      (github.event.issue.author_association == 'FIRST_TIMER' ||
       github.event.issue.author_association == 'FIRST_TIME_CONTRIBUTOR')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v8
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: [
                "Thanks for helping improve The Alpha Cloud.",
                "",
                "Please include the affected deployment model and a reproducible outcome. Security reports belong in a private advisory."
              ].join("\n")
            });

  pull-request:
    name: Welcome first-time pull request authors
    if: >-
      github.event_name == 'pull_request_target' &&
      (github.event.pull_request.author_association == 'FIRST_TIMER' ||
       github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/github-script@v8
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: [
                "Thanks for your first contribution to The Alpha Cloud.",
                "",
                "A maintainer will review the tenant, audit, and approval boundaries after CI finishes."
              ].join("\n")
            });
