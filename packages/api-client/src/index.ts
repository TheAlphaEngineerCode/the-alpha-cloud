/**
 * @cloud/api-client — typed fetch client generated from the API OpenAPI document.
 *
 * PLACEHOLDER — generation runs in Phase 1 when `@fastify/swagger` exposes the spec. Until
 * the codegen emits a real client, the web app uses TanStack Query with hand-typed calls
 * against `/api/v1`.
 */
export {};
