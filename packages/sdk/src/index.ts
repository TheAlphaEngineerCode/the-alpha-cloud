/**
 * @cloud/sdk — public API re-exports for external programmatic integration.
 *
 * PLACEHOLDER until the public `apps/cli` documents surface stabilises (Phase 4+).
 * Re-export path mirrors what the published bundle will expose.
 */
export type * from '@cloud/domain';