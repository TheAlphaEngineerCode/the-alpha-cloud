/**
 * @cloud/ui — shared component registry (shadcn/ui tokens + Cloud-specific primitives).
 *
 * PLACEHOLDER — actual components live in `apps/web/components` for Phase 0. When stable,
 * shared ones migrate here so `apps/cli` (rich TUI), future mobile clients and the docs site
 * can reuse them. Empty on purpose; importing symbols at this stage yields nothing.
 */
export {};